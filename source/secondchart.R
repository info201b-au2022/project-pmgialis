# Second chart for project

library(ggplot2)
library("plotly")

library(ggplot2)

data.frame(df)

graph2 <- ggplot(data = df) +
  geom_polygon(
    mapping = aes(x = sl, y = sr.1),
    color = "light blue")
graph2

